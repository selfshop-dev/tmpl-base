name: Release Drafter

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    types: [ opened, reopened, synchronize, labeled, unlabeled, closed ]
    branches: [ main ]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: selfshop-dev/lib-default-workflows/.github/workflows/release-drafter.yml@main
    secrets: inherit